<% if admin?(current_user) %>

  <h1>Editing Place</h1>

  <%= render 'form', place: @place %>

  <%= link_to 'Show', @place %> |
  <%= link_to 'Back', places_path %>
<%else%>
  <br><br>
  Access Denied
<%end%>
